interface:
  display_name: "Relay Images"
  short_description: "Generate or edit relay images with optional Lark delivery"
  default_prompt: "Use $relay-images to create or edit an image through my configured relay, use Lark artifact delivery on a weak link, and save the verified result locally."
